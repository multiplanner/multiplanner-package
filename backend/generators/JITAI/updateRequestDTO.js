
module.exports = (item, Item, ITEM, items, Items, ITEMS, api) => ``/*
public class Update${Item}RequestDTO extends ${Item}RequestDTO {
    private int id;

    public Update${Item}RequestDTO() {
    }

    public Create${Item}RequestDTO(int id, ${constructorArgumenten(api)}) {
        ${superCall(api)}
    }
}
`;
*/