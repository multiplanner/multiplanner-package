
module.exports = (item, Item, ITEM, items, Items, ITEMS, api) => ``/*
public class Create${Item}RequestDTO extends ${Item}RequestDTO {

    public Create${Item}RequestDTO() {
    }

    public Create${Item}RequestDTO(${constructorArgumenten(api)}) {
        ${superCall(api)}
    }
}
`;
*/