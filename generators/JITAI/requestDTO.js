module.exports = (item, Item, ITEM, items, Items, ITEMS, api) => ``/*


public abstract class ${Item}RequestDTO${extend(api, bami => `${bami}RequestDTO`, `RequestDTO`)} {
    ${klassenvariabeleDeclaratiesAnnotated(api)}
    public ${Item}RequestDTO() {
    }
    
    // amongus
    public ${Item}RequestDTO(${constructorArgumenten(api)}) {${superCall(api)}
        ${klassenvariabeleInstantiaties(api)}
    }
    ${getters(api)}
    ${setters(api)}
}

`;*/
